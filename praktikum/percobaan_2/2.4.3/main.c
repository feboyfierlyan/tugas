#include <stdio.h>

void main() {
    int a = 12;
    int b = 8;
    int hasil;
    float c = 11.0;
    float d = 7.0;

    printf("6 + a / 5 * b = %d\n\n", 6 + a / 5 * b);
    printf("a / b * b = %d\n\n", a / b * b);
    printf("c / d * d = %f\n\n", c / d * d);
    printf("-a = %d\n\n", -a);

}